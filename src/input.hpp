#ifndef INPUT_HPP
#define INPUT_HPP

#include <vector>
#include <string>

namespace input {
    std::vector<std::string> readInputFile(std::string);
    std::vector<std::string> readParametersInput();
}

#endif
